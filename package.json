{
  "name": "video-platform-app",
  "version": "1.0.0",
  "private": true,
  "description": "Video Data Collection & Multi-Role Vendor Management Platform",
  "scripts": {
    "start": "npx concurrently \"npm --prefix backend run dev\" \"npm --prefix admin-dashboard run dev\" \"npm --prefix vendor-dashboard run dev\"",
    "dev": "npx concurrently \"npm --prefix backend run dev\" \"npm --prefix admin-dashboard run dev\" \"npm --prefix vendor-dashboard run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
